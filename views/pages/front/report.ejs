<h2 class="inset content-title"><span class="icon-flag"></span>&nbsp;&nbsp;Report</h2>

<div class="outset">
  <div class="outset-box">
    <p>
      Your opinion is (or, better, <i>can be</i> if you're not trolling) valuable. Thanks for taking some time to fill this report!
    </p>

    <form id="report-form" method="post" action="/report/submit">
      <div id="report-type" class="grid-4 input-group">
        <div class="cell-1">
          <button type="button" class="btn" data-value="bug"><span class="icon-bug"></span><span class="hidden-mobile">&nbsp;Bug</span></button>
        </div>
        <div class="cell-1">
          <button type="button" class="btn" data-value="feature"><span class="icon-brightness-medium"></span><span class="hidden-mobile">&nbsp;Feature</span></button>
        </div>
        <div class="cell-1">
          <button type="button" class="btn" data-value="comment"><span class="icon-heart"></span><span class="hidden-mobile">&nbsp;Comment</span></button>
        </div>
        <div class="cell-1">
          <button type="button" class="btn" data-value="other"><span class="icon-question"></span><span class="hidden-mobile">&nbsp;Other</span></button>
        </div>
      </div>
      <div class="clear"></div>

      <textarea name="content" placeholder="Tell me what happened!" rows="8"><% if ('undefined' !== typeof previousFormData && 'undefined' !== typeof previousFormData.content) { %><%= previousFormData.content %><% } %></textarea>

      <div class="grid-12 input-group">
        <div class="cell-10">
          <input type="text" name="author" <% if ('undefined' !== typeof previousFormData && 'undefined' !== typeof previousFormData.author) { %>value="<%= previousFormData.author %>"<% } else { %>value="Anonymous"<% } %> placeholder="Name..." />
        </div>
        <div class="cell-2">
          <button class="btn submit-button">Send!</button>
        </div>
      </div>
      <div class="clear"></div>
    </form>
  </div>
</div>

<!-- Bottom padding from footer -->
<div class="page-section-padding"></div>
<!-- !Bottom padding from footer -->
